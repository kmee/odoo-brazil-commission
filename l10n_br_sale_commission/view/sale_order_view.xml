<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_sales_to_invoice_order_agent_form_view" model="ir.ui.view">
        <field name="name">view.sales.order.agent.form.view</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sped_sale.sale_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook[last()]" position="inside">
                <page string="Comissão" name="commission_page" >
                        <field name="agents" />
                </page>
            </xpath>
        </field>
    </record>

    <record model="ir.ui.view" id="view_sale_order_line_tree">
        <field name="name">sale.order.line.agent.tree</field>
        <field name="model">sale.order.line.agent</field>
        <field name="arch" type="xml">
            <tree string="Agents" editable="bottom">
                <field name="agent"/>
                <field name="commission"/>
            </tree>
        </field>
    </record>

</odoo>
